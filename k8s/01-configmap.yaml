apiVersion: v1
kind: ConfigMap
metadata:
  name: cash-or-card-config
data:
  POSTGRES_DB: cash_or_card
  POSTGRES_USER: postgres
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  NODE_ENV: production
  PORT: "3001"
  # Frontend Config
  REACT_APP_API_URL: "http://backend:3001" # This might need to be the public URL in production
