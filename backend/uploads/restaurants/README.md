# Restaurant Images

This directory contains uploaded restaurant images.

Image files are named using the restaurant's UUID: `{restaurant_id}.jpg`

## Default Image

If a restaurant doesn't have a dedicated image, the frontend will fall back to a default image.
